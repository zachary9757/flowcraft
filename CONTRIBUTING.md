# Contributing

Keep changes surgical and add a reproducing test for behavior changes. Run
`bash tests/run.sh` before submitting a change. Kernel installation and qdisc
changes require an additional disposable Linux VPS validation report.
