# Security

Do not report security issues in public issues. Contact the repository owner
privately. FlowCraft runs as root; configuration values must remain whitelist
parsed and must never be evaluated as shell code.
